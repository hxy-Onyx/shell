操作符（|），用于将一个命令的标准输出（stdout）直接传递给另一个命令的标准输入（stdin）。
6.1基本概念：
标准输出（stdout）：命令执行后产生的输出结果。
标准输入（stdin）：命令接收的输入数据。
管道符号（|）：连接两个命令，将前一个命令的输出作为后一个命令的输入。
6.2管道的工作原理
当您在Shell中使用管道符号（|）连接两个命令时，Shell会：
1.创建两个子进程：分别执行命令1和命令2。
2.建立管道：将命令1的标准输出连接到命令2的标准输入。
3.数据传输：命令1执行时产生的输出会实时传输给命令2作为输入。
4.并行执行：两个命令通常会并行运行，数据在它们之间流动。
示例流程：
命令1 | 命令2
命令1 产生输出。
命令2 接收来自 命令1 的输出作为输入。
命令2 处理输入并产生最终结果。
